# Spotify Folder Documentation Alignment

## Task Summary

The Spotify Data Analysis project was previously placed inside an extra `t` folder by mistake. That folder has been removed manually, so the documentation needs to match the real project location.

Current correct folder:

```text
tasks/Spotify Data Analysis/
```

## Phase Plan

### Phase 1: Check Current Documentation

Goal: Find any references to the old Spotify folder path.

Work to do:

- Read `CODEx.md`.
- Read `AGENTS.md`.
- Search the repository for references to the old extra `t` folder.

Teaching focus:

- Documentation should match the actual folder structure so future work goes into the right place.

Status: Completed.

Progress:

- Found the old folder path in `AGENTS.md`.
- Found the old folder path in the Spotify project phase document.
- Confirmed the actual folder is `tasks/Spotify Data Analysis/`.

### Phase 2: Update Documentation

Goal: Replace the old mistaken folder path with the correct current path.

Work to do:

- Update `AGENTS.md`.
- Update `CODEx.md` if needed so the main workflow instructions are explicit.
- Update the Spotify phase document so its progress notes do not point to the removed folder.

Teaching focus:

- A path is the written address of a file or folder. If a path is wrong, future commands or project work can end up in the wrong place.

Status: Completed.

Progress:

- Updated `AGENTS.md` to point to `tasks/Spotify Data Analysis/`.
- Updated `CODEx.md` with an explicit repository organization section.
- Updated the Spotify project phase document so its progress note points to the correct folder.

### Phase 3: Verify The Cleanup

Goal: Make sure no documentation still points to the removed `t` folder.

Work to do:

- Search the repository again for references to the old extra `t` folder.
- Check that the project folders still exist in the correct location.

Teaching focus:

- Verification means checking your work after making a change, instead of assuming the change worked.

Status: Completed.

Progress:

- Searched the repository again.
- Confirmed the active Spotify path is `tasks/Spotify Data Analysis/`.
- Confirmed the expected project folders exist: `data/`, `notebooks/`, `outputs/`, `notes/`, and `phases/`.

## Final Checklist

- [x] Phase document created before the main documentation edits.
- [x] Existing documentation reviewed.
- [x] Documentation updated to use `tasks/Spotify Data Analysis/`.
- [x] Repository searched again for the old path.
- [x] User told how to check the result.
